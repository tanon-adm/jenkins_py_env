pipeline {
    agent any
    stages {
        stage('Calling the cmd-script') { 
            environment {
                TECH_USER_PSW = credentials('tech_user')
            }
            steps { 
                script {
                    bat returnStatus: true, script: $/script.bat/$
                } 
            } 
        }
    }
}
